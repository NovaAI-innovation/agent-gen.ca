version: '3.8'
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: agentgen
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql+asyncpg://postgres:password@db:5432/agentgen
    depends_on:
      - db
    volumes:
      - .:/app

volumes:
  postgres_data:
